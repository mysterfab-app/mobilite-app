{
  "name": "mobilite-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd api && npm install && cd ../front && npm install",
    "build": "cd front && npm run build",
    "start": "cd api && node app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
